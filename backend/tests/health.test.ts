import { describe, expect, it } from "vitest";

describe(
  "Health Check",
  () => {
    it(
      "should confirm the test environment works",
      () => {
        expect(true).toBe(true);
      }
    );
  }
);